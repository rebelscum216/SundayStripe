import { ChannelWorkspace } from "../channel-workspace";

export default function AmazonPage() {
  return (
    <ChannelWorkspace
      platform="amazon_sp"
      title="Amazon"
      meta="SP-API listing quality"
    />
  );
}
